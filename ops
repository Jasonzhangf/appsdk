{"author":{"id":"aefab16712695e4f092f7bad7c1285ac48ce0d1ef6cc283cea20b3795ed9457a"},"ops":[{"type":1,"timestamp":1788762589,"nonce":"5WaIuUmboYgSIJefrS8rXgYXIbc=","title":"[Collab Bug] sendmessage 不应强制发件人具备接收能力/tmux-pane；已有协作ID自动填入","message":"### Problem\n1. 客户端限制：`collab sendmessage` 强制调用 `identity::load_or_create`，在无 `TMUX_PANE` 环境（如标准终端、CI 脚本、无 pane 外部调用）下直接报错：`collab identity requires a live tmux pane`。\n2. 服务端限制：`src/server/mod.rs` 处理 `Req::Send` 时校验 `if runtime_for_pane(sender.pane.as_deref()).is_none() { return Resp::err(\"sender has no valid tmux pane\"); }`，硬性要求发件人必须具备接收唤醒的能力。\n3. 根因缺陷：发送消息本质是向收件人 Mailbox/Journal 写入记录的单向操作，与接收/被唤醒能力（需 live pane + safe idle）正交。不应强制要求发件人拥有 live tmux pane。\n\n### Expected Behavior\n1. 已有协作 ID 时自动填入：若当前存在有效协作 ID（如 `COLLAB_WORKER`、tmux 环境或注册身份），发送时自动推导并填入 `from`，无需手动指定。\n2. 无接收能力/无 pane 环境正常发送：在外部终端、脚本或非 tmux 环境下，支持通过 `--from \u003cname\u003e` 显式指定，或自动 fallback 为轻量发件人标识（如 `operator` / `$USER`），不再因无 tmux pane 阻断发信。\n3. 服务端解耦：服务端移除对 `sender.pane` 的强制要求（只保留对收件人 `to` 的校验与防风暴防护），允许无 pane 发件人向已注册收件人合法写信。\n\n### Repro / Verification Command\n- 外部环境发送（当前会失败）：`env -u TMUX_PANE collab sendmessage --to \u003cpeer\u003e --subject test \"msg\"`\n- 期望结果：发信成功，进入收件人 mailbox，收件人正常被唤醒。","files":null}]}